<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MarvelApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="toolbarStyle">@style/MarvelToolbar</item>
    </style>

    <!-- Global toolbar style -->
    <style name="MarvelToolbar" parent="@style/Widget.Material3.Toolbar">
        <item name="android:elevation">4dp</item>
    </style>

    <style name="Theme.MarvelApp" parent="Base.Theme.MarvelApp" />
</resources>